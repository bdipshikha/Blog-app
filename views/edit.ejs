<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>blog.title</title>
</head>
	<body>
			<h3>Update <%= thisPost.title %> here:</h3>
			<form action ="/blog/<%= thisPost.id %>?_method=PUT" method="POST">
			<input type="text" name="id" value="<%= thisPost.id %>" required>
			<input type="text" name="title" value="<%= thisPost.title %>" required>
			<textarea name="content" required>"<%= thisPost.content %>"</textarea>
			<input type="text" name="image" value="<%= thisPost.image %>" required>
			<button>Submit</button>
			</form>
			<a href="/">Back to blog</a>
	</body>
</html> 


