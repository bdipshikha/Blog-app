<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>blog.title</title>

	<style>
		h1 {
			text-align: center;
			color: #F00;
			font-family: chalkduster;
		}
		body {
			text-align: left;
			background-color: tan;
			margin: 30px;
			padding: 0px 10px 5px;
		}
		a {
			color: red;
		}
		p {
			text-align: justify;
			color: blue;
			font-family: times new roman;
			font-size: 18px;
		}
	</style>

</head>
<body>
	<h3>Update <%= thisPost.title %> here:</h3>
	<form action ="/blog/<%= thisPost.id %>?_method=PUT" method="POST">
		<input type="text" name="title" value="<%= thisPost.title %>" required></br>
		<textarea rows="15" cols="130" name="content" required><%= thisPost.body %></textarea></br> 
		<!-- needs to be body not content -->
		<input type="text" name="image" value="<%= thisPost.image %>" required>
		<button>Submit</button>
	</form>
	<a href="/">Back to blog</a>
</body>
</html> 


